# Supabase — Project Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-public-key

# Google AI Studio — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# Optional: override the default model, and/or set a fallback model that gets
# used automatically if the primary one hits a rate-limit/quota/server error
# (429/500/503). Leave GEMINI_FALLBACK_MODEL blank to disable failover. When
# you buy paid-tier credits, pointing GEMINI_MODEL at a stronger model is the
# only change needed — no code edits.
# GEMINI_MODEL=gemini-flash-lite-latest
# GEMINI_FALLBACK_MODEL=gemini-flash-latest

# Comma-separated list of origins allowed to call this API
ALLOWED_ORIGINS=http://localhost:5173,https://your-username.github.io

# How many days of daily_logs/water_logs to keep before they're purged
# (rolling window, not calendar weeks). weight_logs is never purged — see
# sql/schema.sql. Keep this in sync with the interval in cleanup_old_logs().
# RETENTION_DAYS=7

# Soft cap on shared daily Gemini calls (vision scans + text-only
# re-estimates) across ALL users — protects the single free-tier API key
# behind this app. There's no universal "correct" number: check your actual
# quota for GEMINI_MODEL in Google AI Studio and set this comfortably under
# it. The value below is a placeholder, not a real observed limit.
# GEMINI_DAILY_QUOTA=1000

# Optional: free Sentry project DSN (sentry.io) for backend error tracking.
# Leave blank to disable entirely — nothing else changes.
# SENTRY_DSN=

# Note: Turnstile (CAPTCHA on signup) has no backend env var — it's
# configured entirely in the Supabase dashboard (Authentication → Attack
# Protection), with its own secret key that never touches this backend. See
# frontend/js/config.js for the public site key the frontend needs.
